import { Context } from "koa";
import { RulePresets } from "./utils/RulePresets";

// 这个文件用于调试配置文件中的规则表达式
const evaluateRuleExpression = (ctx: Context, presets: RulePresets): any => {
    return false;
}